-- show databases;
CREATE table data(id int not null primary key,username varchar(20),emailid varchar(20));

insert into data(id,username,emailid) VALUES (1,'admin','prajapatidinesh313@gmail');

SELECT * from data;
